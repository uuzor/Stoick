{
  "name": "wraith",
  "version": "0.1.0",
  "private": true,
  "description": "Full-privacy platform on Stellar — bridge, portfolio, private pay, ZK dark pool.",
  "license": "MIT",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "dependencies": {
    "@aztec/bb.js": "^5.0.0"
  }
}
