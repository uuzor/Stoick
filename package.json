{
  "name": "dysentry",
  "version": "0.1.0",
  "private": true,
  "description": "Dysentry private CLMM and shielded exchange platform on Stellar.",
  "license": "MIT",
  "packageManager": "pnpm@10.11.0",
  "scripts": {
    "dev": "pnpm --filter @dysentry/clmm-frontend dev --host 0.0.0.0 --port 3000",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "dependencies": {
    "@aztec/bb.js": "^5.0.0"
  }
}
