[package]
name = "clmm_mint"
version = "0.1.0"
edition = "2024"
type = "bin"
description = "CLMM mint circuit - add liquidity to a concentrated position"

[dependencies]
poseidon = { tag = "v0.2.0", git = "https://github.com/noir-lang/poseidon" }
wraith_lib = { path = "../wraith_lib" }
wraith_clmm_lib = { path = "../wraith_clmm_lib" }
